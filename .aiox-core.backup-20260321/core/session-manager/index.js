'use strict';

const { SessionManager } = require('./session-manager');

/**
 * Session Manager Module
 *
 * Exports the SessionManager class for persistent agent session storage
 *
 * @module aiox-core/session-manager
 * @version 1.0.0
 */

module.exports = {
  SessionManager,
};
