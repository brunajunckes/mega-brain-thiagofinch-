'use strict';

const BookProcessor = require('./book-processor');
const BookExporter = require('./book-exporter');
const BookIntegrator = require('./book-integrator');

module.exports = {
  BookProcessor,
  BookExporter,
  BookIntegrator,
};
