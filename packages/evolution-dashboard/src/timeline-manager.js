'use strict';

// Re-export from core module for backward compatibility
const { TimelineManager } = require('../../../.aiox-core/core/evolution-dashboard/timeline-manager');

module.exports = TimelineManager;
